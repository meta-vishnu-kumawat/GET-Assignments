/*
 * For each table below we are applying constraints to ensure data integrity:
 * - PRIMARY KEY: Uniquely identifies each record.
 * - NOT NULL: Ensures required columns always have a value.
 * - UNIQUE: Enforces uniqueness (e.g., Email in Shopper).
 * - FOREIGN KEY: Enforces referential integrity between related tables.
 ***************************************************************************/